model works better when there are only 2 hidden layers

TIME_STEP = 0.01
n_epochs = 30
lr = 0.1
hiddenSize = 50
hiddenLayer = 2
optimizer = torch.optim.LBFGS(model.parameters(), lr=lr)

needs more epochs to finish